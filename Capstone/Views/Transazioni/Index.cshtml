@model IEnumerable<Capstone.Models.Transazioni>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NFT.NomeFile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Utenti.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Utenti1.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Importo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataTransazione)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NFT.NomeFile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utenti.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utenti1.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Importo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataTransazione)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTransazione }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTransazione }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTransazione })
        </td>
    </tr>
}

</table>

@model IEnumerable<Capstone.Models.FileNFT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeFile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoFile)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeFile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoFile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFileNFT }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFileNFT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFileNFT })
        </td>
    </tr>
}

</table>
